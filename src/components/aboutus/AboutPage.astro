---
import Layout from '../../layouts/Layout.astro'
import { useTranslations, getTranslation } from '../../utils/translations'

export interface Props {
  locale?: string
}

// ✨ Una sola línea para cargar traducciones!
const { locale, translations, t } = await useTranslations(Astro.props, Astro)

// 🎯 Helper para traducciones más elegante (opcional)
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<Layout
  title={`${tr('about.title', 'Acerca de')} - ${tr('site.title', 'Smart Technology Expo')}`}
  description={tr('about.subtitle', 'Conoce más sobre Smart Technology Expo')}
>
  <div class='about-container'>
    <header class='about-header'>
      <h1>{tr('about.title', 'Acerca de Nosotros')}</h1>
      <p class='subtitle'>
        {tr('about.subtitle', 'Conoce nuestra misión y visión')}
      </p>
    </header>
    <div class='about-content'>
      <!-- Misión -->
      <section class='section'>
        <div class='card'>
          <h2>{tr('about.mission.title', 'Nuestra Misión')}</h2>
          <p>
            {
              tr(
                'about.mission.description',
                'Conectar líderes tecnológicos con innovaciones.'
              )
            }
          </p>
        </div>
      </section>

      <!-- Visión -->
      <section class='section'>
        <div class='card'>
          <h2>{tr('about.vision.title', 'Nuestra Visión')}</h2>
          <p>
            {
              tr(
                'about.vision.description',
                'Ser la plataforma líder mundial en tecnología.'
              )
            }
          </p>
        </div>
      </section>

      <!-- Valores -->
      <section class='section'>
        <div class='card'>
          <h2>{tr('about.values.title', 'Nuestros Valores')}</h2>
          <p>
            {
              tr(
                'about.values.description',
                'Nuestros valores fundamentales son la innovación, la colaboración y la sostenibilidad.'
              )
            }
          </p>
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .about-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  .about-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .about-header h1 {
    font-size: 3rem;
    color: #2c3e50;
    margin-bottom: 1rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: #7f8c8d;
    max-width: 600px;
    margin: 0 auto;
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .section {
    width: 100%;
  }

  .card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-left: 4px solid #3498db;
  }

  .card h2 {
    color: #2c3e50;
    margin-bottom: 1rem;
    font-size: 1.8rem;
  }

  .card p {
    color: #34495e;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .value-item {
    text-align: center;
    padding: 1rem;
    background: #f8f9fa;
    border-radius: 8px;
  }

  .value-item h3 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .value-item p {
    color: #2c3e50;
    font-weight: 500;
    margin: 0;
  }

  @media (max-width: 768px) {
    .about-container {
      padding: 1rem;
    }

    .about-header h1 {
      font-size: 2rem;
    }

    .values-grid {
      grid-template-columns: 1fr;
    }

    .card {
      padding: 1.5rem;
    }
  }
</style>

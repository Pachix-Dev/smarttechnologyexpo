---
// Componente compartido para todas las páginas de inicio
import Welcome from './Welcome.astro'
import Layout from '../../layouts/Layout.astro'
import { useTranslations, getTranslation } from '../../utils/translations'
import Description from './Description.astro'
import StandPrice from './StandPrice.astro'
import Highlights from './Highlights.astro'
import { events } from '../../data/constans_events'
import Events from './Events.astro'
import ContactTeam from './ContactTeam.astro'

export interface Props {
  locale?: string
}

// ✨ Una sola línea para cargar traducciones!
const { translations } = await useTranslations(Astro.props, Astro)
// 🎯 Helper para traducciones más elegante
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<Layout
  title={tr('site.title', 'Smart Technology Expo')}
  description={tr('site.description', 'Expo de tecnología inteligente')}
>
  <Welcome />
  <Description />
  <StandPrice />
  <ContactTeam />
  <Highlights />
  <Events events={events} isLeftToRight={true} />
</Layout>

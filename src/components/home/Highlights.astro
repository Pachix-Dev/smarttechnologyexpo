---
import { useTranslations, getTranslation } from '../../utils/translations'
// ✨ Una sola línea para cargar traducciones!
const { translations } = await useTranslations(Astro.props, Astro)
// 🎯 Helper para traducciones más elegante
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<section
  class='bg-[url(/img/bg_smart_desc_2.png)] bg-no-repeat bg-center bg-cover py-16 px-4 text-center'
>
  <div class='container mx-auto px-4 py10'>
    <p class='text-2xl md:text-5xl text-center text-white'>
      <strong>Smart Technology Expo</strong>
      {tr('home.description')}
    </p>
    {
      /* 
      <div class='mt-10 grid md:grid-cols-4 gap-8 mx-auto text-[#dc0c15]'>
        <div class='bg-white p-2 grid items-center'>
          <p class='text-2xl font-bold'>275 {tr('home.exhibitors')}</p>
          <p>{tr('home.hightlights_1')}</p>
        </div>
        <div class='bg-white p-2 grid items-center'>
          <p class='text-2xl font-bold'>+30 {tr('home.countries')}</p>
          <p>{tr('home.hightlights_2')}</p>
        </div>
        <div class='bg-white p-2 grid items-center'>
          <p class='text-2xl font-bold'>+50 {tr('home.Conferences')}</p>
          <p>{tr('home.hightlights_3')}</p>
        </div>
        <div class='bg-white p-2 grid items-center'>
          <p class='text-2xl font-bold'>+15,000 {tr('home.visitors')}</p>
          <p>{tr('home.hightlights_4')}</p>
        </div>
      </div>
      */
    }
  </div>
</section>

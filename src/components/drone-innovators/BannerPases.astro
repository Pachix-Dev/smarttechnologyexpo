---
// src/components/drone-innovators/BannerPases.astro
import { useTranslations, getTranslation } from '../../utils/translations'

export interface Props {
  locale?: string
}

const { locale, translations } = await useTranslations(Astro.props, Astro)
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<section class="bp-section">

  <!-- Fondo — imagen dicta la altura, sin recortes -->
  <img
    src="/img/drone-innovators/bg-bannerpases.webp"
    alt=""
    class="bp-bg"
    aria-hidden="true"
  />

  <!-- Botón pill centrado sobre el fondo -->
  <div class="bp-inner" data-aos="zoom-in" data-aos-duration="700">
    <a href="#" class="bp-btn">
      {tr('droneInnovators.bannerPases.cta', '¡ADQUIERE TUS PASES!')}
    </a>
  </div>

</section>

<style>
  .bp-section {
    position: relative;
    width: 100%;
    background: #0b1627;
  }

  .bp-bg {
    display: block;
    width: 100%;
    height: auto;
  }

  .bp-inner {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 4rem;
    box-sizing: border-box;
  }

  .bp-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #fff;
    color: #111;
    font-family: 'Montserrat', sans-serif;
    font-size: clamp(1rem, 4.5vw, 3.9rem);
    font-weight: 900;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    text-decoration: none;
    padding: 1.1rem 4rem;
    border-radius: 60px;
    white-space: nowrap;
    transition: background 0.2s, color 0.2s, transform 0.2s;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.2);
  }

  .bp-btn:hover {
    background: #f0f0f0;
    transform: translateY(-2px);
  }

  /* ── DESKTOP ≥1281px ── */
  @media (min-width: 1281px) {
    .bp-inner { padding: 0 4rem; }
    .bp-btn   { font-size: 3.9rem; padding: 1.1rem 4rem; border-radius: 60px; white-space: nowrap; }
  }

  /* ── TABLET GRANDE 1025px–1280px ── */
  @media (max-width: 1280px) and (min-width: 1025px) {
    .bp-inner { padding: 0 4rem; }
    .bp-btn   { font-size: 3rem; padding: 1rem 3.5rem; border-radius: 55px; white-space: nowrap; }
  }

  /* ── TABLET 769px–1024px ── */
  @media (max-width: 1024px) and (min-width: 769px) {
    .bp-inner { padding: 0 3rem; }
    .bp-btn   { font-size: 2.4rem; padding: 1rem 3rem; border-radius: 50px; white-space: nowrap; }
  }

  /* ── MOBILE ≤768px ── */
  @media (max-width: 768px) {
    .bp-inner { padding: 0 2rem; }
    .bp-btn   { font-size: 1.6rem; padding: 0.9rem 2.5rem; border-radius: 40px; white-space: normal; text-align: center; }
  }

  /* ── MOBILE PEQUEÑO ≤480px ── */
  @media (max-width: 480px) {
    .bp-inner { padding: 0 1.5rem; }
    .bp-btn   { font-size: 1.2rem; padding: 0.8rem 2rem; border-radius: 30px; white-space: normal; text-align: center; }
  }

  /* ── MOBILE MUY PEQUEÑO ≤380px ── */
  @media (max-width: 380px) {
    .bp-inner { padding: 0 1rem; }
    .bp-btn   { font-size: 1rem; padding: 0.7rem 1.5rem; border-radius: 24px; white-space: normal; text-align: center; }
  }
</style>
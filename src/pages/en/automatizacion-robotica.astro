---
// src/pages/en/automatizacion-robotica.astro
import Layout from '../../layouts/Layout.astro'
import SegmentoLayout from '../../layouts/SegmentoLayout.astro'
import { useTranslations, getTranslation } from '../../utils/translations'

export interface Props {
  locale?: string
}

const { locale, translations } = await useTranslations({ locale: 'en' }, Astro)
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<Layout
  title="Automation and Robotics — Smart Technology Expo 2026"
  description="Discover the Automation and Robotics segment at Smart Technology Expo 2026."
>
  <SegmentoLayout
    locale="en"
    titulo="Automation and Robotics"
    descripcion={tr('home.new_cap', 'Coming soon: information about this segment.')}
    icono="/img/segmentos/Automatizacion_Robótica.webp"
    proximamente={true}
    expositoresBuscados={[]}
  />
</Layout>
---
// src/pages/en/drone-innovators.astro
import Layout from '../../layouts/Layout.astro'
import SegmentoLayout from '../../layouts/SegmentoLayout.astro'
import { useTranslations, getTranslation } from '../../utils/translations'

export interface Props {
  locale?: string
}

const { locale, translations } = await useTranslations({ locale: 'en' }, Astro)
const tr = (key: string, fallback: string = '') =>
  getTranslation(translations, key, fallback)
---

<Layout
  title="Drone Innovators Forum — Smart Technology Expo 2026"
  description="Discover the Drone Innovators Forum at Smart Technology Expo 2026."
>
  <SegmentoLayout
    locale="en"
    titulo="Drone Innovators Forum"
    descripcion={tr('home.new_cap', 'Coming soon: information about this segment.')}
    icono="/img/segmentos/Drone_Innovators.webp"
    proximamente={true}
    expositoresBuscados={[]}
  />
</Layout>